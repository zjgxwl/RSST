# 启动日志使用指南

## 📁 目录说明

`launch_logs/` 目录用于存放实验启动时的配置日志，与 `experiments/` 目录互补：

| 目录 | 用途 | 文件大小 | 保存内容 |
|------|------|----------|----------|
| **launch_logs/** | 启动日志 | 1-2 KB | 配置信息、启动确认、进程ID |
| **experiments/** | 完整结果 | MB-GB | 训练日志、模型权重、检查点 |

---

## 🚀 启动实验的两种方式

### 方式一：使用 `launch_experiment.sh`（推荐）

**自动保存启动日志到 `launch_logs/` 目录**

```bash
# 基本用法（与run_experiment.sh参数相同）
./launch_experiment.sh <dataset> <model> <epochs> <reg> <exp> <pretrained> <rate> <auto> <algorithm> <fillback>

# 示例1: RSST算法
./launch_experiment.sh cifar10 vit_tiny 80 1.0 4 true 0.2 y rsst

# 示例2: Refill算法
./launch_experiment.sh cifar100 vit_tiny 80 1.0 4 true 0.2 y refill 0.2

# 日志文件自动命名为: cifar10_rsst_20260109_143025.log
```

### 方式二：手动指定日志路径

```bash
# 自定义日志文件名
nohup ./run_experiment.sh cifar10 vit_tiny 80 1.0 4 true 0.2 y rsst \
  > launch_logs/my_experiment.log 2>&1 &

# 使用时间戳命名
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
nohup ./run_experiment.sh cifar100 vit_tiny 80 1.0 4 true 0.2 y refill 0.2 \
  > launch_logs/cifar100_refill_${TIMESTAMP}.log 2>&1 &
```

---

## 🔍 查看启动日志

```bash
# 列出所有启动日志
ls -lh launch_logs/

# 查看特定日志
cat launch_logs/cifar10_rsst_20260109_143025.log

# 查看最新的启动日志
ls -t launch_logs/*.log | head -1 | xargs cat

# 同时查看多个日志
tail launch_logs/*.log
```

---

## 📊 从启动日志获取信息

每个启动日志包含：

1. **实验配置**
   - 数据集、模型、算法
   - 超参数（epochs、rate、reg等）
   
2. **实验目录**
   - 例如：`experiments/20260109_143025_rsst_vit_tiny_cifar10_pretrained_80ep/`
   
3. **进程信息**
   - 进程ID（用于停止实验）
   
4. **监控命令**
   - 完整训练日志路径

### 示例：提取进程ID

```bash
# 从日志中提取进程ID
grep "进程ID:" launch_logs/cifar10_rsst_*.log

# 停止该实验
kill <进程ID>
```

---

## 🗑️ 清理旧日志

```bash
# 查看日志文件数量和总大小
ls -lh launch_logs/ | grep ".log" | wc -l
du -sh launch_logs/

# 删除7天前的日志
find launch_logs/ -name "*.log" -mtime +7 -delete

# 删除30天前的日志
find launch_logs/ -name "*.log" -mtime +30 -delete

# 只保留最新的10个日志
cd launch_logs && ls -t *.log | tail -n +11 | xargs rm -f
```

---

## 📋 日志文件命名规范

建议使用以下格式：

```
<dataset>_<algorithm>_<timestamp>.log
```

**示例**：
- `cifar10_rsst_20260109_143025.log`
- `cifar100_refill_20260109_150030.log`
- `imagenet_rsst_20260110_080000.log`

---

## 🔗 与完整日志的关系

**启动日志** → 找到实验目录 → 查看**完整训练日志**

```bash
# 1. 查看启动日志，获取实验目录
cat launch_logs/cifar10_rsst_20260109_143025.log

# 2. 从中找到实验目录（例如）：
#    experiments/20260109_143025_rsst_vit_tiny_cifar10_pretrained_80ep/

# 3. 查看完整训练日志
tail -f experiments/20260109_143025_rsst_vit_tiny_cifar10_pretrained_80ep/logs/stdout.log

# 或使用快捷链接
tail -f experiments/latest/logs/stdout.log
```

---

## 💡 最佳实践

1. **使用 `launch_experiment.sh`**
   - 自动保存启动日志
   - 统一的命名格式
   - 更好的可追溯性

2. **定期清理**
   - 启动日志很小但会累积
   - 建议每月清理一次旧日志

3. **保存重要实验的启动日志**
   - 对于论文实验，保留启动日志
   - 便于复现和记录

4. **配合 experiments/ 目录使用**
   - 启动日志：快速查看配置
   - 完整日志：分析训练过程

---

## 🎯 快速参考

| 操作 | 命令 |
|------|------|
| 启动实验 | `./launch_experiment.sh cifar10 vit_tiny 80 1.0 4 true 0.2 y rsst` |
| 列出日志 | `ls -lh launch_logs/` |
| 查看日志 | `cat launch_logs/cifar10_rsst_*.log` |
| 查看最新 | `ls -t launch_logs/*.log \| head -1 \| xargs cat` |
| 清理旧日志 | `find launch_logs/ -name "*.log" -mtime +30 -delete` |
| 提取进程ID | `grep "进程ID:" launch_logs/*.log` |
